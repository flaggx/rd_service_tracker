// Re-export useAuth and AuthProvider from the context.
// Also provide a default export for convenience.
export { useAuth, AuthProvider } from "../context/AuthProvider";
export { useAuth as default } from "../context/AuthProvider";